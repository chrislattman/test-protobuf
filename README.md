# Protocol Buffers (protobuf) examples

Make sure to run `sudo apt install protobuf-compiler` (tested with version 3.21.12)

Use the provided Makefile to run the examples. Before running the C++ application, run `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`
